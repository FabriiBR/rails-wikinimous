<%= form_for(@article) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :content %>
  <%= t.text_field :content %>
  <br>
  <%= t.submit %>
<% end %>
<%= link_to "Back to home", articles_path %>